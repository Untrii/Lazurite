//import './CompactRadio.scss'
import { h } from 'preact'

interface ICompactRadioProps {
  prepend?: string
  variants
}

const CompactRadio = (props: ICompactRadioProps) => {
  return
}
export default CompactRadio
